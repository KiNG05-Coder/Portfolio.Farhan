# Normalise line endings: store LF in the repo, check out native.
* text=auto eol=lf

# Treat these as binary (never touch line endings).
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.mp4   binary
*.woff  binary
*.woff2 binary
